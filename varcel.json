{
    "version": 1,
    "builds": [
     {
      "src": "bin/www",
      "use": "@vercel/node",
      "config": { "includeFiles": ["bin/**", "config/**", "database/**", "functions/**", "public/**", "routes/**", "validator/**"] }
     }
    ],
    "routes": [
     {
       "src": "/(.*)",
       "dest": "bin/www"
     }
    ]
}